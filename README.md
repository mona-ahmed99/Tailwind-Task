# Tailwind Dashboard

A responsive React dashboard built with Vite and Tailwind CSS.

- Uses CSS variables for theme colors.
- Dark mode implemented using Tailwind class strategy.
- Theme preference stored in localStorage.
- Reusable components created with @layer components.
- Components: Button, Card, Input, Badge.
- Custom utilities added for layout consistency.
- Responsive layout using CSS Grid.


